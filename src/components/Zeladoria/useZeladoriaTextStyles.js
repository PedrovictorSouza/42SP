export function useZeladoriaTextStyles() {
  return {
    fontFamily: "'Space Grotesk', sans-serif",
    fontSize: 'clamp(1rem, 1.5vw, 1.2rem)',
    fontWeight: 400,
    color: '#ffffff',
    lineHeight: 1.6,
    margin: 0,
    paddingLeft: 'calc(clamp(1.5rem, 2.5vw, 2rem) + 1rem)'
  }
}

